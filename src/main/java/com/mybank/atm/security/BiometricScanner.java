package com.mybank.atm.security;

public class BiometricScanner implements IBiometricScanner{
    public boolean authenticateUser(String userId) {
        // Authenticate user using biometric data
        return false;
    }
}
